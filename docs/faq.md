# FAQ

Q: Why does it fallback?
A: No `GOOGLE_API_KEY` is found, or model loading fails at runtime.

Q: How to rebuild FAISS index?
A: Delete `faiss_index/` and rerun script.

Q: How to run tests?
A: `pytest` after adding tests.
